name: "Build aws-efs-utils"

description: "Build utilities for Amazon Elastic File System (EFS) for RHEL 9"

inputs:
  version:
    description: "The version to build"
    required: true

runs:
  using: "docker"
  image: "Dockerfile"
